version: '3.8'

services:
  bono-navideno:
    build: .
    ports:
      - "8080:80"
    container_name: bono-navideno-app
    restart: unless-stopped
    environment:
      - NGINX_PORT=80
    networks:
      - bono-network

networks:
  bono-network:
    driver: bridge
